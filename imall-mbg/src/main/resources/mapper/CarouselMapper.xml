<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yangnan.mall.mapper.CarouselMapper">

  <resultMap id="BaseResultMap" type="com.yangnan.mall.pojo.Carousel">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="image_url" jdbcType="VARCHAR" property="imageUrl" />
    <result column="link_url" jdbcType="VARCHAR" property="linkUrl" />
    <result column="sort" jdbcType="INTEGER" property="sort" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>

  <sql id="Base_Column_List">
    id, image_url, link_url, sort, create_time, update_time
  </sql>

  <insert id="save" parameterType="com.yangnan.mall.pojo.Carousel">
    INSERT INTO `carousel` (`img_url`, `link_url`) VALUES (#{imgUrl}, #{linkUrl})
  </insert>

  <delete id="deleteById">
    DELETE FROM `carousel` WHERE `id` = #{id}
  </delete>

  <update id="update" parameterType="com.yangnan.mall.pojo.Carousel">
    UPDATE `carousel` SET `img_url` = #{imgUrl}, `link_url` = #{linkUrl} WHERE `id` = #{id}
  </update>

  <select id="getById" parameterType="int" resultType="com.yangnan.mall.pojo.Carousel">
    SELECT * FROM `carousel` WHERE `id` = #{id}
  </select>

  <select id="getAll" resultType="com.yangnan.mall.pojo.Carousel">
    SELECT * FROM `carousel`
  </select>

  <select id="selectByPage" parameterType="CarouselQuery" resultType="com.yangnan.mall.pojo.Carousel">
    select
    <include refid="Base_Column_List" />
    from carousel
  </select>

  <insert id="insertSelective" parameterType="com.yangnan.mall.pojo.Carousel">
    insert into carousel
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="id != null">
        id,
      </if>
      <if test="imageUrl != null">
        image_url,
      </if>
      <if test="linkUrl != null">
        link_url,
      </if>
      <if test="sort != null">
        sort,
      </if>

      <if test="createTime != null">
        create_time,
      </if>
      <if test="updateTime != null">
        update_time,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="id != null">
        #{id,jdbcType=INTEGER},
      </if>
      <if test="imageUrl != null">
        #{imageUrl,jdbcType=VARCHAR},
      </if>
      <if test="linkUrl != null">
        #{linkUrl,jdbcType=VARCHAR},
      </if>
      <if test="sort != null">
        #{sort,jdbcType=INTEGER},
      </if>

      <if test="createTime != null">
        #{create_time,jdbcType=TIMESTAMP},
      </if>
      <if test="updateTime != null">
        #{update_time,jdbcType=TIMESTAMP},
      </if>
    </trim>
  </insert>


  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from carousel
    where id = #{id,jdbcType=INTEGER}
  </select>

  <update id="updateByPrimaryKeySelective" parameterType="com.yangnan.mall.pojo.Carousel">
    update carousel
    <set>
      <if test="id != null">
        id = #{id,jdbcType=INTEGER},
      </if>
      <if test="imageUrl != null">
        `image_url` = #{imageUrl,jdbcType=VARCHAR},
      </if>
      <if test="linkUrl != null">
        link_url = #{linkUrl,jdbcType=VARCHAR},
      </if>

      <if test="sort != null">
        `sort` = #{sort,jdbcType=INTEGER},
      </if>

      <if test="createTime != null">
        create_time = #{createTime,jdbcType=TIMESTAMP},
      </if>
      <if test="updateTime != null">
        update_time = #{updateTime,jdbcType=TIMESTAMP},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from carousel
    where id = #{id,jdbcType=INTEGER}
  </delete>

  <delete id="deleteAll">
    delete from carousel
    where id IN
    <foreach collection="array" open="(" close=")" item="id" separator=",">
      #{id}
    </foreach>
  </delete>

  <select id="selectAll" resultType="com.yangnan.mall.pojo.Carousel">
    select
    <include refid="Base_Column_List" />
    from carousel
    order by sort asc
  </select>

</mapper>